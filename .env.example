# docker-compose 환경변수 템플릿
# 실제 사용 시 이 파일을 .env 로 복사한 뒤 값을 채워 넣으세요.
#   cp .env.example .env
# .env 는 .gitignore 처리되어 커밋되지 않습니다.

# JWT 서명용 비밀키 — 최소 256bit(32자) 이상의 랜덤 문자열 사용
# 생성 예시:  openssl rand -base64 48
JWT_SECRET=
